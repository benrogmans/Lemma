[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "business_rules"
version = "0.1.0"
description = "Hand-written Python ports of Lemma benchmark business rules."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["business_rules*"]
