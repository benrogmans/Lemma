# API fixtures

Canonical JSON documents for Show, Response, list, and EngineError.

Coverage is derived from `documentation/schemas/api.v1.json`: every schema-enumerated
variant must appear in at least one fixture here. Bindings parse these same files.
